package test_Astar;

import java.util.Comparator;

public class ComparaBase implements Comparator<Quadrado> {

	@Override
	public int compare(Quadrado o1, Quadrado o2) {
		
		return Integer.compare(o1.getPoder(), o2.getPoder());
	}

}
